# Temporary Working Notes

This is a **disposable, reusable scratch file** — not a permanent doc. It
holds dev-session comments/investigation notes for whichever quest is
currently under **active development** (not yet FINAL LOCK). Once that
quest reaches FINAL LOCK again, its entries here get filtered: real
findings move to `docs/bugs.md` (engine/SDK bugs) or `docs/source-current.md`
(design/story rationale), anything that was just noise gets dropped, and
this file goes back to empty (or gets reused for whichever quest is active
next).

**Currently scoped to: none** — Q04 reached FINAL LOCK on 2026-09-17 (see
`docs/bugs.md` entries 29-34 and `docs/source-current.md`'s Q04 section
for what was extracted from here). Empty until the next quest goes into
active development.
